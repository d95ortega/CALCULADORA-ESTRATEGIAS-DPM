{
  "entities": {
    "AuthorizedUser": {
      "title": "Authorized User",
      "description": "A user authorized to access the company's application",
      "type": "object",
      "properties": {
        "email": { "type": "string", "format": "email", "description": "The user's email address" },
        "role": { "type": "string", "enum": ["admin", "user"], "description": "The user's role (admin or user)" },
        "addedAt": { "type": "string", "description": "When the user was added to the whitelist" }
      },
      "required": ["email", "role"]
    },
    "Customer": {
      "title": "Customer",
      "description": "A client of the business",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The customer's full name" },
        "phone": { "type": "string", "description": "The customer's phone number" },
        "email": { "type": "string", "format": "email", "description": "The customer's email address" },
        "address": { "type": "string", "description": "The customer's physical address" },
        "notes": { "type": "string", "description": "Any additional notes about the customer" },
        "createdAt": { "type": "string", "description": "The date the customer was first registered" },
        "quotesCount": { "type": "number", "description": "The total number of quotes generated for this customer" }
      },
      "required": ["name", "phone", "createdAt"]
    },
    "Quote": {
      "title": "Quote",
      "description": "A formal quote generated for a customer",
      "type": "object",
      "properties": {
        "customerName": { "type": "string", "description": "The name of the customer" },
        "customerPhone": { "type": "string", "description": "The phone number of the customer" },
        "date": { "type": "string", "description": "The date the quote was generated" },
        "total": { "type": "number", "description": "The total investment amount of the quote" },
        "items": { "type": "array", "description": "The list of items included in the quote" },
        "orderId": { "type": "string", "description": "The ID of the order if this quote was converted" }
      },
      "required": ["customerName", "date", "total", "items"]
    },
    "Order": {
      "title": "Order",
      "description": "A production order generated from a quote",
      "type": "object",
      "properties": {
        "quoteId": { "type": "string", "description": "The ID of the source quote" },
        "customerName": { "type": "string", "description": "The name of the customer" },
        "customerPhone": { "type": "string", "description": "The phone number of the customer" },
        "total": { "type": "number", "description": "The total amount of the order" },
        "status": { "type": "string", "enum": ["NUEVA", "CORTE_LASER", "PLOTTER_CORTE", "BODEGA_FABRICA", "BODEGA_PUNTO_VENTA", "ENTREGADO", "DESCARGADOS"], "description": "The current production status" },
        "createdAt": { "type": "string", "description": "When the order was created" },
        "updatedAt": { "type": "string", "description": "When the order status was last updated" },
        "items": { "type": "array", "description": "The list of items in the order" }
      },
      "required": ["quoteId", "customerName", "total", "status", "createdAt", "updatedAt", "items"]
    }
  },
  "firestore": {
    "/authorized_users/{email}": {
      "schema": "AuthorizedUser",
      "description": "The list of emails authorized to access the application"
    },
    "/company_data/dpm/customers/{customerId}": {
      "schema": "Customer",
      "description": "The shared collection of customers for the company"
    },
    "/company_data/dpm/quotes/{quoteId}": {
      "schema": "Quote",
      "description": "The shared collection of quote history for the company"
    },
    "/company_data/dpm/orders/{orderId}": {
      "schema": "Order",
      "description": "The shared collection of production orders for the company"
    },
    "/company_data/dpm/settings/current": {
      "schema": "object",
      "description": "The shared settings (brand, params, products) for the company"
    }
  }
}
